---
vm_name: "{{ vmname | upper }}"
vm_state: "{{ vmstate }}"
vm_memory: "{{ vmmemory }}"
vm_cpu: "{{ vmcpu }}"
vm_generation: "{{ vmgeneration }}"
vm_switch: "{{ vmswitch }}"
vm_osvhdsize_bytes: "{{ vmosvhdsize_bytes }}"
vm_osdiskpath: "{{ vmosdiskpath }}"